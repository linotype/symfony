{% extends "@Linotype/admin.twig" %}

{% block content %}
    
    <h2 class="pt-2">Blocks - <a href="{{block.link}}">view</a></h2>
    <div class="columns">
        {% for item in block.items %}
        <div class="column p-1 col-4 col-xs-12">
            <div class="card">
                {# <iframe 
                    class="preview-frame"
                    title="Inline Frame Example"
                    width="100%"
                    height="100%"
                    scrolling="no"
                    src="{{item.link}}/preview/?mode=iframe">
                </iframe> #}
                <div class="card-header">
                    <div class="dropdown float-right">
                        <div class="btn-group"><a href="{{item.link}}"
                                class="btn btn-primary">Edit</a><a
                                class="btn btn-primary dropdown-toggle" tabindex="0"><i
                                    class="icon icon-caret"></i></a>
                            <ul class="menu">
                                <li class="menu-item"><a href="">View</a></li>
                                </li>
                                <li class="menu-item"><a href="">Delete</a></li>
                            </ul>
                        </div>
                    </div>
                    <div class="card-title h5">{{ item.name }}</div>
                    <div class="card-subtitle text-gray">v.<span class="version">{{ item.version|number_format(1) }}</span> by <span class="package">{{ item.author }}</span></div>
                </div>
                <div class="card-body">{{ item.desc }}</div>
            </div>
        </div>
        {% endfor %}
        </div>

        <h2 class="pt-2">Fields - <a href="{{field.link}}">view</a></h2>
        <div class="columns">
        {% for item in field.items %}
            <div class="column p-1 col-4 col-xs-12">
                <div class="card">
                    {# <iframe 
                        class="preview-frame"
                        title="Inline Frame Example"
                        width="100%"
                        height="100%"
                        scrolling="no"
                        src="{{item.link}}/preview/?mode=iframe">
                    </iframe> #}
                    <div class="card-header">
                        <div class="dropdown float-right">
                            <div class="btn-group"><a href="{{item.link}}"
                                    class="btn btn-primary">Edit</a><a
                                    class="btn btn-primary dropdown-toggle" tabindex="0"><i
                                        class="icon icon-caret"></i></a>
                                <ul class="menu">
                                    <li class="menu-item"><a href="">View</a></li>
                                    </li>
                                    <li class="menu-item"><a href="">Delete</a></li>
                                </ul>
                            </div>
                        </div>
                        <div class="card-title h5">{{ item.name }}</div>
                        <div class="card-subtitle text-gray">v.<span class="version">{{ item.version|number_format(1) }}</span> by <span class="package">{{ item.author }}</span></div>
                    </div>
                    <div class="card-body">{{ item.desc }}</div>
                </div>
            </div>
        {% endfor %}
        </div>

        <h2 class="pt-2">Helpers - <a href="{{helper.link}}">view</a></h2>
        <div class="columns">
        {% for item in helper.items %}
            <div class="column p-1 col-4 col-xs-12">
                <div class="card">
                    {# <iframe 
                        class="preview-frame"
                        title="Inline Frame Example"
                        width="100%"
                        height="100%"
                        scrolling="no"
                        src="{{item.link}}/preview/?mode=iframe">
                    </iframe> #}
                    <div class="card-header">
                        <div class="dropdown float-right">
                            <div class="btn-group"><a href="{{item.link}}"
                                    class="btn btn-primary">Edit</a><a
                                    class="btn btn-primary dropdown-toggle" tabindex="0"><i
                                        class="icon icon-caret"></i></a>
                                <ul class="menu">
                                    <li class="menu-item"><a href="">View</a></li>
                                    </li>
                                    <li class="menu-item"><a href="">Delete</a></li>
                                </ul>
                            </div>
                        </div>
                        <div class="card-title h5">{{ item.name }}</div>
                        <div class="card-subtitle text-gray">v.<span class="version">{{ item.version|number_format(1) }}</span> by <span class="package">{{ item.author }}</span></div>
                    </div>
                    <div class="card-body">{{ item.desc }}</div>
                </div>
            </div>
        {% endfor %}
        </div>

        <h2 class="pt-2">Modules - <a href="{{module.link}}">view</a></h2>
        <div class="columns">      
        {% for item in module.items %}
            <div class="column p-1 col-4 col-xs-12">
                <div class="card">
                    {# <iframe 
                        class="preview-frame"
                        title="Inline Frame Example"
                        width="100%"
                        height="100%"
                        scrolling="no"
                        src="{{item.link}}/preview/?mode=iframe">
                    </iframe> #}
                    <div class="card-header">
                        <div class="dropdown float-right">
                            <div class="btn-group"><a href="{{item.link}}"
                                    class="btn btn-primary">Edit</a><a
                                    class="btn btn-primary dropdown-toggle" tabindex="0"><i
                                        class="icon icon-caret"></i></a>
                                <ul class="menu">
                                    <li class="menu-item"><a href="">View</a></li>
                                    </li>
                                    <li class="menu-item"><a href="">Delete</a></li>
                                </ul>
                            </div>
                        </div>
                        <div class="card-title h5">{{ item.name }}</div>
                        <div class="card-subtitle text-gray">v.<span class="version">{{ item.version|number_format(1) }}</span> by <span class="package">{{ item.author }}</span></div>
                    </div>
                    <div class="card-body">{{ item.desc }}</div>
                </div>
            </div>
        {% endfor %}
        </div>

        <h2 class="pt-2">Templates - <a href="{{template.link}}">view</a></h2>
        <div class="columns">
        {% for item in template.items %}
            <div class="column p-1 col-4 col-xs-12">
                <div class="card">
                    {# <iframe 
                        class="preview-frame"
                        title="Inline Frame Example"
                        width="100%"
                        height="100%"
                        scrolling="no"
                        src="{{item.link}}/preview/?mode=iframe">
                    </iframe> #}
                    <div class="card-header">
                        <div class="dropdown float-right">
                            <div class="btn-group"><a href="{{item.link}}"
                                    class="btn btn-primary">Edit</a><a
                                    class="btn btn-primary dropdown-toggle" tabindex="0"><i
                                        class="icon icon-caret"></i></a>
                                <ul class="menu">
                                    <li class="menu-item"><a href="">View</a></li>
                                    </li>
                                    <li class="menu-item"><a href="">Delete</a></li>
                                </ul>
                            </div>
                        </div>
                        <div class="card-title h5">{{ item.name }}</div>
                        <div class="card-subtitle text-gray">v.<span class="version">{{ item.version|number_format(1) }}</span> by <span class="package">{{ item.author }}</span></div>
                    </div>
                    <div class="card-body">{{ item.desc }}</div>
                </div>
            </div>
        {% endfor %}
        </div>

        <h2 class="pt-2">Themes - <a href="{{theme.link}}">view</a></h2>
        <div class="columns">       
        {% for item in theme.items %}
            <div class="column p-1 col-4 col-xs-12">
                <div class="card">
                    {# <iframe 
                        class="preview-frame"
                        title="Inline Frame Example"
                        width="100%"
                        height="100%"
                        scrolling="no"
                        src="{{item.link}}/preview/?mode=iframe">
                    </iframe> #}
                    <div class="card-header">
                        <div class="dropdown float-right">
                            <div class="btn-group"><a href="{{item.link}}"
                                    class="btn btn-primary">Edit</a><a
                                    class="btn btn-primary dropdown-toggle" tabindex="0"><i
                                        class="icon icon-caret"></i></a>
                                <ul class="menu">
                                    <li class="menu-item"><a href="">View</a></li>
                                    </li>
                                    <li class="menu-item"><a href="">Delete</a></li>
                                </ul>
                            </div>
                        </div>
                        <div class="card-title h5">{{ item.name }}</div>
                        <div class="card-subtitle text-gray">v.<span class="version">{{ item.version|number_format(1) }}</span> by <span class="package">{{ item.author }}</span></div>
                    </div>
                    <div class="card-body">{{ item.desc }}</div>
                </div>
            </div>
        {% endfor %}
        </div>         

    </div>

{% endblock %}
